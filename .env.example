# ─── Frontend (Vite) ──────────────────────────────────────────────────────────
# Disponible côté navigateur — ne jamais mettre de secrets ici
VITE_SUPABASE_URL=https://wjbkizdzyllveypcngqq.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable__hAERh09tYTzb298JtAjBA_VosGoO1S

# ─── Supabase Edge Functions (secrets à ajouter dans le dashboard Supabase) ───
# supabase secrets set ANTHROPIC_API_KEY=sk-ant-api03-...
# supabase secrets set STRIPE_SECRET_KEY=sk_live_...
# supabase secrets set STRIPE_WEBHOOK_SECRET=whsec_...
# supabase secrets set STRIPE_PRICE_STARTER=price_...
# supabase secrets set STRIPE_PRICE_PRO=price_...
# supabase secrets set STRIPE_PRICE_AUTODIDACTE=price_...
# supabase secrets set STRIPE_PRICE_TEACHER=price_...
# supabase secrets set ALLOWED_ORIGIN=https://learni.app
